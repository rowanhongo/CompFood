// Configuration file for Comp Food app
const CONFIG = {
    // API endpoints
    CHAT_API: '/api/chat',
    FOOD_PRICES_API: '/api/food-prices',
    
    // App settings
    APP_NAME: 'Comp Food',
    VERSION: '1.0.0',
    
    // Default settings
    DEFAULT_CURRENCY: 'USD',
    DEFAULT_CONTINENT: 'all'
};

// Make config available globally
window.CONFIG = CONFIG; 